# Integrated Platform UI/UX Design System

本文档定义了本项目的全局 UI 视觉规范与交互设计原则。
**无论是人类开发者还是 AI Agent，在新增页面或重构组件时，都必须优先阅读并严格遵守此规范。**

---

## 1. 核心设计哲学 (Core Philosophy)

本平台是一个重度工程与数据计算系统，设计需遵循以下三大基调：
- **高信息密度 (High Data Density)**：一屏内尽可能展示更多的有效工程数据，反对过度留白、大字号的“消费级”UI 设计。
- **效率优先 (Efficiency First)**：减少鼠标点击与跨区域移动，所有的输入、计算、核对流程应能通过快捷键（Enter/Esc/Tab）或极简交互闭环。
- **专业与克制 (Professional & Restrained)**：界面应像专业的 IDE 或 Excel，不打扰、不花哨，仅在必要时提供辅助。

---

## 2. 交互设计规约 (Interaction Principles)

在处理表格、表单和配置面板时，必须遵守以下“类 Excel”交互准则：

### 2.1 静态优先，局部激活 (Static First, Local Activation)
- **规则**：绝不为了提供编辑能力而渲染出成排的带边框表单组件（如常驻的 `<el-input>` 或 `<el-select>`）。
- **执行**：默认态永远是纯文本或 `code` 标签；仅在用户的明确意图（双击）下，才将目标单元格或文本**原地替换**为轻量级的输入控件，且严禁波及同行其他单元格（例如：双击计算块/场景标题，将其原地替换为重命名输入框，敲击 Enter 完成）。

### 2.2 所见即所得，零视觉跳动 (Zero Visual Jitter)
- **规则**：在文本展示态与控件编辑态之间切换时，严控尺寸变化。
- **执行**：输入控件的 `font-size`、`padding`、`line-height` 必须与其静态文本展示态高度对齐。切忌使用撑大行高、改变列宽的输入框。

### 2.3 操作的隐性闭环 (Implicit Action Closure)
- **规则**：永远不要强迫用户去寻找孤立的“保存”或“取消”按钮。
- **执行**：所有表单输入都必须支持 `Enter` (提交)、`Esc` (取消) 以及 `Click-outside` (失焦智能比对并保存)。显式的 Check/Close 图标仅作为新手的视觉兜底。

### 2.4 辅助信息的克制 (Restrained Assistance)
- **规则**：不要在用户不需要帮助的时候强塞信息。
- **执行**：联想词 (Autocomplete)、参数提示、语法高亮等面板必须满足**“按需触发（敲击匹配字符） + 绝对定位悬浮 + 随焦点消失”**。坚决抵制在组件挂载时就一股脑弹出的冗余 UI。

### 2.5 焦点即真理 (Focus is Truth)
- **规则**：用户的光标在哪里，哪里就是当前唯一的系统级状态。
- **执行**：全局状态管理器必须清晰记录 `activeKey`（高亮实体）和 `editingField`（编辑属性）。确保在任意时刻，系统里最多只有一个处于激活/被编辑状态的原子组件，支持无缝的焦点直接转移覆盖。

### 2.6 增删对称与闭环 (Symmetry & Closure of Operations)
- **规则**：任何实体一旦提供创建/添加能力，就必须同时提供对应的删除/归档入口。
- **执行**：功能不能只做一半。UI 上必须提供低视觉干扰但明确的删除操作点（如 Hover 时显示的 Danger Text 按钮）。对于聚合类实体（如包含子公式的计算块），执行删除前必须强制引入二次确认拦截（如 `ElMessageBox.confirm`）以防误触导致大量数据丢失。即使是刚创建的空容器（如未添加任何公式的空计算块），也必须能够独立被删除，绝不能因为没有子元素就缺失删除入口。

### 2.7 弱干扰的操作入口 (Low-Distraction Action Entries)
- **规则**：添加和删除操作不能成为干扰视线的东西，不要时时刻刻都显示纯文字按钮。
- **执行**：使用图标替代纯文字的“添加/删除”按钮，并做出互动的方便性评价（如 Tooltip 提示）。操作图标默认隐藏（或透明度 0），仅在鼠标靠近（Hover 父容器）或键盘聚焦时才显示。

### 2.8 响应式数据的绝对控制 (Absolute Reactivity Control)
- **规则**：复杂状态的更新与组件间的数据传递，必须保证 100% 触发视图重渲染并精准溯源。
- **执行**：
  1. 优先使用解构赋值/不可变数据模式（Immutable patterns，如 `[...arr, new]`）更新深层数组和对象，坚决避免使用 `.push()` 等可能导致响应式丢失的方法。
  2. 下发给 UI 渲染列表（尤其是基于 Computed 生成的聚合列表）的每一行数据，必须携带完整的上下文标识（如 `moduleCode`、`sceneCode`），绝不能只传递展示用的 `label`，否则将导致 UI 抛出的事件缺少关键追溯锚点。

### 2.9 跨组件交互闭环 (Cross-Component Interaction Closure)
- **规则**：允许通过外部面板直接干预当前激活组件的状态，提升输入效率。
- **执行**：
  1. 在特定编辑场景下（如处于公式输入状态），点击外部参数列表时，不应触发常规的“查看详情”逻辑，而是智能判断当前上下文，将参数直接追加到当前光标位置，实现交互闭环，减少不必要的复制粘贴。
  2. 这种“编辑态点击参数即插入”的能力必须被视为公式编辑器的基础能力，严禁因为重构状态机、切换工作台版本或补充其他保存逻辑而意外丢失。凡是涉及 `activeKey`、`editingField`、光标位置、参数面板点击事件的修改，都必须回归验证“编辑中点击参数可插入公式”。
  3. 当系统检测到当前存在活跃公式输入框时，参数点击事件必须优先服务于插入行为；只有在未处于编辑态时，才允许退回到参数详情、解释面板或高亮查看等只读行为。

### 2.10 公式编辑态的“类 Excel”沉浸式交互 (Excel-like Formula Editing Immersion)
- **规则**：公式编辑过程应提供类似 Excel 单元格编辑的沉浸式体验，支持跨区域点击取值，且不轻易打断编辑心流。
- **执行**：
  1. **跨区取值不失焦**：当处于公式编辑态时，点击页面上的“输入参数”、“已选型设备”、“结果值”或“其他公式”等可计算参数区域，必须将对应的标准参数名（`paramName`）精准插入到当前公式的光标位置，且**绝对不能触发编辑框的失焦（Blur）或退出编辑状态**。
  2. **空白区域安全退出**：只有当用户点击页面上的非参数区域（如背景空白处、非交互面板）时，才允许触发 `click-outside` 事件，执行自动保存或取消逻辑，并安全退出编辑状态。
  3. **事件拦截与穿透**：在实现 `click-outside` 指令或全局点击监听时，必须精确识别并拦截目标 DOM 节点（如 `.equipment-param-item`, `.summary-card`, `.workbench-input-table__row` 等），确保这些区域的点击事件能够穿透并服务于“参数插入”逻辑，而非被误判为“点击外部”。

### 2.11 上下文变量的隐式注册与鲁棒提取 (Implicit Context Variable Registration & Robust Extraction)
- **规则**：公式引擎必须具备高鲁棒性，能够自动接纳用户书写的任何合规标识符，绝不允许因为变量未预先定义而抛错打断编辑心流。
- **执行**：
  1. 无论是前端语法校验还是后端计算请求，公式引擎在处理表达式时，必须通过正则（如 `/[A-Za-z_\u00C0-\uFFFF][A-Za-z0-9_\u00C0-\uFFFF]*\s*(?!\()/`）动态扫描并提取所有的合法非函数标识符。
  2. 提取出的未知参数应被隐式注册至计算作用域（Scope）并自动赋予安全的兜底值（如 `0` 或 `1`）。
  3. 这种“基于特征正则的动态提取替换”，能够确保带有未定义新参数的合法公式也能顺利通过解析树并算出占位结果，彻底解除“保存必须先录入参数”的强时序依赖。
  4. 公式保存链路不能只保存裸 `expression` 字符串；在保存前后都必须显式维护“公式 -> 变量依赖 -> 参数绑定”的闭环。前端应在提交前根据表达式自动生成 `variables`，后端也必须按相同规则兜底补齐并校验，不能单点信任前端已有数据。
  5. `variables` 的前后端数据契约必须保持稳定且唯一，统一使用可序列化的键值结构（如 `Dict[str, str]`）。严禁在某些页面写入字符串、另一些页面写入对象，导致同一公式在不同工作台版本下出现“编辑可过、保存报错”或“旧数据可算、新数据不可存”的隐性分叉。

### 2.12 联想与语法校验的一致性 (Autocomplete and Syntax Consistency)
- **规则**：联想弹出的候选值必须与当前主推的函数语法严格兼容，不应因为向下兼容旧语法而向用户呈现可能导致校验失败的脏数据。
- **执行**：对于查表附录等数据源，其联想项的 `value` 必须保持纯净的实体名称（如仅返回 `电机扭矩参数`，而不应硬编码 Excel 坐标 `!B:C`）。确保在使用 `CURVE2D` 等推荐公式时，用户直接点击联想结果不会触发“曲线表不存在”的解析异常。旧语法的向下兼容应交由特定场景策略处理，绝不能污染全局联想字典。

### 2.13 工况校核与结果对比 (Condition Verification & Result Comparison)
- **规则**：校核模块的计算结果不应作为普通的输出参数并列展示，而应与被校核的理论值（或目标参数）进行绑定对比，并通过直观的视觉反馈呈现校核状态。
- **执行**：
  1. **规则配置**：在公式的说明面板（Explanation Panel）中，支持添加“校核规则（Verification Rule）”类型的资源。允许用户选择目标参数（理论值），并设定对比关系（如 `>`、`<`、`==`、`between` 等）及允许误差。
  2. **数据过滤**：被配置为校核规则的公式，其计算结果不应再作为独立的“结果卡片”展示，而是作为实际值，附加到目标参数的结果卡片上。
  3. **视觉反馈**：结果卡片需根据校核规则的计算结果动态改变样式。校核通过（Pass）时卡片呈现绿色调（如 `is-pass` 类），校核不通过（Fail）时呈现红色调（如 `is-fail` 类），未配置校核规则的卡片保持默认蓝色调。
  4. **卡片结构**：结果卡片内部需清晰划分“实际（Actual）”与“理论（Theory）”区域，直观展示两者的数值对比。

### 2.14 智能选型的极简紧凑与状态持久化 (Compactness & Persistence in Smart Selection)
- **规则**：智能选型结果卡片必须紧凑、克制，杜绝大面积留白与冗长的行列；一旦应用了选型结果，必须持久化保存该状态（型号与关键性能参数），确保下次打开或刷新时选型结果不丢失。
- **执行**：
  1. **极简结果呈现**：选型结果展示需摒弃占满整行的瀑布式表单。在参数面板中，对于有推荐选型的参数，Hover 触发轻量级浮层列表即可。对于专门的“智能选型”标签页或卡片，应采用高密度的 Inline 布局（如将电机和减速机参数作为小型 Badge 或紧凑的 Grid 排列），只展示最核心的几个数字（如功率、转速、减速比），次要详情折叠。
  2. **选型结果持久化**：用户点击“应用”某款设备型号后，系统不仅要将对应的数值（转速、扭矩等）覆盖当前公式输入，还必须将**完整的设备选型记录（Equipment Selection Record，含具体型号代号与关键规格）** 作为工作台参数快照或独立实体持久化到本地（LocalStorage）或后端。
  3. **重新加载还原**：在刷新页面或重新进入设计工作台时，系统需从存储中读取已保存的设备选型记录，并在面板显眼位置（如参数列表顶部或独立小卡片）常驻展示“当前已选设备”，以确认该方案已绑定特定物理硬件，直至用户主动清除或替换。

### 2.15 硬件参数的计算域对齐与数据校验 (Hardware Parameter Alignment & Data Validation)
- **规则**：设计平台的理论计算结果往往与真实物理硬件（如电机）的额定参数存在微小偏差（例如理论所需电机转速 1488 rpm，真实电机额定转速为 1477 rpm）。在智能选型和计算链路中，必须建立明确的校验与对齐机制，杜绝“假数据”或隐性误差。
- **执行**：
  1. **真实数据绝对优先**：在选型数据库和匹配算法中，严禁为了强行凑平理论计算值而生成或伪造不存在的硬件参数。推荐结果必须 100% 来源于真实的设备参数表。
  2. **差异告警与校验机制**：当智能选型模块探测到当前用户输入的“理论所需参数”（如电机额定转速）与即将推荐的“真实硬件参数”之间存在偏差时，必须在 UI 层面（如卡片醒目位置或 Hover Tooltip）提供明确的 **数据不一致告警（Mismatch Warning）**，明确告知用户差异的存在。
  3. **自动对齐与回写**：用户应用选型结果后，系统必须用物理硬件的真实参数（如 1477 rpm）覆盖替换掉原先的理论输入值（如 1488 rpm），驱动工作台公式引擎基于真实参数进行二次校核（Re-calculation），确保整个计算链路的数据严谨对齐。

### 2.16 界面分析与解释文字的“信噪比”控制 (Signal-to-Noise Ratio in Analysis & Explanation UI)
- **规则**：工作台的任何分析界面或参数列表，都必须具备极高的“信噪比”，仅呈现与当前上下文强相关的元素，坚决剔除毫无意义的“废话”或冗余的全量列表。
- **执行**：
  1. **分析面板聚焦**：在“校核扫描分析”或类似的趋势分析面板中，扫描参数的下拉选项不得无脑罗列所有基础参数。必须通过公式依赖树**反向追溯**，仅列出直接或间接影响该分析结果的输入参数，做到精准制导。
  2. **图表基准分离**：分析图表中的对比数据应在视觉上形成明确的基准。理论值（基准线）应采用低饱和度、弱视觉权重的**虚线**（Dashed Line）绘制，而实际值采用实线；实际值穿透基准线形成的区间，必须通过颜色（如绿/红）**色块（MarkArea）直观划分**合格与不合格的区域底色。
  3. **去除冗余解释词**：列表和表格中（如参数列表、计算列表）应当通过排版结构本身传达含义。必须去除诸如“结果输出”、“包含 N 个输入”等无实际信息的“解释词语”占位，杜绝画蛇添足的占位符。

---

## 3. 视觉与排版 (Visual & Layout)

### 3.1 颜色系统
- **中性灰度**：用于边框、分割线、背景。如 `border: 1px solid #e2e8f0` 或 `#dcdfe6`。
- **主品牌色**：克制使用，仅用于激活态的指示器（Active Border）或关键行动点（Primary Button）。
- **文本颜色**：正文使用深灰（如 `#333` 或 `#1f2937`），次要描述或元数据使用浅灰（如 `#666` 或 `#64748b`）。

### 3.2 字体排版
- **数据与代码**：计算式、参数标识符、数值必须使用等宽字体（`font-family: monospace` 或自定义的 `JetBrains Mono` / `Consolas` 等），以便于对齐和审查。
- **常规文本**：使用系统默认无衬线字体，字号通常控制在 `12px` ~ `14px`。

### 3.3 组件选型偏好
- **复杂展示**：优先使用平铺或分栏结构，替代层层嵌套的卡片（Card）和折叠面板（Collapse）。
- **附加说明**：优先使用右侧抽屉（Drawer），保持主内容区可见；避免使用遮挡视线的全局弹窗（Dialog）。
- **状态反馈**：优先使用局部 Loading 和轻量级 `ElMessage`，避免全局遮罩打断用户心流。
